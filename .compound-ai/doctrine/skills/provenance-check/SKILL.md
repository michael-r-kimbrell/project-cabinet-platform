# provenance-check (retired)
# Merged into release-captain in v3.0.0. No independent session-routable skill.

Provenance verification is Step 3 of the ship gate.
Procedure: `doctrine/skills/release-captain/reference/provenance-verification.md`

Triggers ("verify origin", "check provenance", "is this the official version")
now route to `release-captain`.
