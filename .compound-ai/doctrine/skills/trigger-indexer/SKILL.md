# trigger-indexer (retired)
# Demoted to CI in v3.0.0. No independent session-routable skill.

Registry maintenance procedure:
`doctrine/skills/request-router/reference/registry-maintenance.md`

Registry schema reference:
`doctrine/skills/trigger-indexer/reference/registry-schema.md`

Registry drift is a repo invariant enforced by `enforcement/bin/check-registry-coherence.sh`.
All trigger-indexer trigger phrases removed from the registry.
