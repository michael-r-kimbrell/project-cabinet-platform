# adoption-captain: Adoption Plan Template (Stage 5)

The adoption plan is the written proposal that the operator approves BEFORE
any file is modified. Stage 5 ends when the operator accepts Phase 1 (at
minimum) or declines and the session closes. The agent does not proceed to
Stage 6 without an explicit per-phase approval.

---

## Phased structure

**Phase 1: Additive scaffolding only.** Drop the kit into `.compound-ai/`.
Generate the adoption report. Propose a minimal pointer in the host agent's
instruction file. No behavior changes. No hooks. No existing files modified.

**Phase 2+: Behavior changes.** Each subsequent phase introduces something
that changes how the agent behaves: enabling a hook, adding triggers to an
instruction file, activating a skill. Each phase requires a separate
operator approval before it runs.

The agent proposes Phase 2 and beyond but does not run them until the
operator reviews the Phase 1 results and says yes.

---

## Adoption plan format

Write the plan as a structured document and present it to the operator.
The operator responds per phase. Record the response before acting.

---

```markdown
# Adoption Plan
Date: YYYY-MM-DD
Host project: <absolute path>
Kit version: v2.6.0
Generated by: adoption-captain

---

## Summary

One paragraph: what the project is, what stack was detected, how many
rules were found in Stage 1, how many conflicts (if any) were resolved,
and the overall shape of the adoption (how many skills adopt now vs.
deferred vs. skipped).

Example: "This is a Node.js/TypeScript API project using pnpm. Discovery
found CLAUDE.md (12 rules), no CI configuration, and test/build/lint
commands via package.json scripts. No conflicts were detected. Proposed
adoption: 6 skills adopt now, 3 deferred, 13 skipped for this stack."

---

## Phase 1: Additive scaffolding (no behavior changes)

### What Phase 1 does

1. Creates `.compound-ai/` directory at the project root
2. Copies kit content to `.compound-ai/` (skills, docs, scripts)
3. Writes `discovery-report.md` and `preserve-map.md` to `.compound-ai/`
4. Writes `adoption-report.md` (partial; will be completed in Stage 8)
5. Proposes one-line pointer addition to the host instruction file
   (requires Phase 1 approval; actual edit is shown in "Proposed file
   changes" below)

### What Phase 1 does NOT do

- Does not modify any existing file without showing the exact diff first
- Does not enable any hook
- Does not change agent behavior
- Does not add skill triggers to instruction files (that is Phase 2)

**Operator action required: approve Phase 1 before the agent proceeds.**

---

## Adopted skills (from Stage 4 mapping rubric)

These skills are classified "adopt now" and will be activated in Phase 2.

| Skill | Tier | Classification | Rationale |
|---|---|---|---|
| request-router | T1 | adopt now | [project-specific rationale] |
| goal-runner | T1 | adopt now | [project-specific rationale] |
| memory | T1 | adopt now | [project-specific rationale] |
| token-economist | T1 | adopt now | [project-specific rationale] |
| ... | | | |

**Adapt notes (if any):** List any "adopt with adaptation" skills here,
with a description of the adaptation (e.g. "memory trigger list trimmed to
the 3 resume fields relevant to this project's session state").

---

## Deferred skills

These skills are relevant but not adopted now. Reasons are project-specific.

| Skill | Tier | Classification | Reason deferred |
|---|---|---|---|
| agent-panel-planning | T1 | defer | Solo-agent project; no multi-agent workflow currently |
| release-captain | T1 | defer | No versioned release process yet |
| ... | | | |

---

## Skipped skills

These skills are not relevant for this project.

| Skill | Tier | Classification | Reason skipped |
|---|---|---|---|
| engagement-bootstrap | T1 | skip | Existing project; for new projects only |
| viz | T2 | skip | No visualization work in scope |
| ... | | | |

---

## Conflicts and resolutions

List conflicts from Stage 2 with their recorded resolutions.
If no conflicts, write "No conflicts detected."

| Rule | Source | Resolution | Operator decision |
|---|---|---|---|
| [rule text] | CLAUDE.md:23 | Skip kit hook for this project | Accepted YYYY-MM-DD |

---

## Preserve rules

The following existing project rules will be honored by all adopted skills.
These will appear verbatim in the memory-commit section (Stage 8b).

1. [rule from preserve-map.md]
2. [rule from preserve-map.md]

---

## Proposed file changes

### Files to create (Phase 1)

```
.compound-ai/                          (new directory)
.compound-ai/adoption-report.md        (partial; completed in Stage 8)
.compound-ai/discovery-report.md       (moved from project root)
.compound-ai/preserve-map.md           (moved from project root)
.compound-ai/_skills-index.md          (generated skill registry)
.compound-ai/AGENT.md                  (kit operating contract)
.compound-ai/doctrine/tiers/            (copied from kit)
.compound-ai/doctrine/skills/      (copied from kit)
```

### Files to modify (Phase 1 -- minimal pointer only)

**`CLAUDE.md`** (or whichever host instruction file is active):

Diff:
```diff
+ <!-- compound-ai:start v2.6.0 -->
+ ## Compound AI Operating Standards
+
+ Kit installed at `.compound-ai/`. Full operating contract: `.compound-ai/AGENT.md`.
+ Adopted skills and triggers will be added in Phase 2.
+
+ <!-- compound-ai:end -->
```

This is a minimal pointer only. The full memory-commit section (with skills,
triggers, and preserve rules) is written in Stage 8b after validation passes.

### Files to enable (Phase 2+)

List which hooks, triggers, or instruction expansions will be proposed in
Phase 2. The operator approves Phase 2 after reviewing Phase 1 results.

- Enable `hooks/pre-commit/token-budget-check.sh` (if detected in stack)
- Add skill trigger table to `CLAUDE.md` memory-commit section
- ...

---

## Validation commands

The following commands will run in Stage 7 to confirm Phase 1 does not
break anything. These are the commands detected in Stage 1.

| Category | Command | Source |
|---|---|---|
| test | `pnpm run test` | package.json scripts.test |
| build | `pnpm run build` | package.json scripts.build |
| lint | `pnpm run lint` | package.json scripts.lint |
| typecheck | `pnpm run typecheck` | package.json scripts.typecheck |

If any command is NOT-FOUND, that gap is noted in the validation report.
It does not block Phase 1 adoption (the agent cannot validate what was
never declared), but it is recorded.

---

## Memory-commit plan (Stage 8b)

After Phase 1 and validation, the following instruction surfaces will
receive the full `## Compound AI Operating Standards` section:

| Surface | Path | Type | Risk |
|---|---|---|---|
| CLAUDE.md | `./CLAUDE.md` | Project-level | Medium |

The memory-commit section will include:
- Adopted skill table (from "Adopted skills" above)
- Preserve rules (from "Preserve rules" above)
- Validation commands (from "Validation commands" above)
- Pointer to `.compound-ai/AGENT.md`

Preview of the section: [agent presents the draft section here, bounded by
markers, for operator review before the actual write]

---

## Operator decision

Phase 1 approval:
[ ] Accept Phase 1 -- proceed with creating .compound-ai/ and minimal pointer
[ ] Decline -- abort adoption; no changes made to project

(Phase 2 and beyond require separate approval after Phase 1 results are visible.)
```

---

## Presenting the plan

The agent presents the full adoption plan in one message, then waits. The
operator's response determines what happens next:

- "Accept Phase 1" or equivalent: proceed to Stage 6 for Phase 1 actions only
- "Accept all phases": record this; still proceed phase by phase and confirm
  at each phase boundary before acting
- "Decline": record the decision in a minimal adoption report noting no changes
  were made; end the session

The agent does not interpret silence as acceptance.

---

## Anti-patterns

### Anti-pattern: running Phase 2 without explicit approval

**Symptom.** Operator approves Phase 1. Agent completes Phase 1 and
immediately begins modifying CLAUDE.md with the full skills section without
asking.

**Fix.** Phase boundary = explicit operator confirmation required. After
Phase 1 completes and validation passes, the agent presents Phase 2 as a
new proposal. The operator approves or declines Phase 2 separately.

### Anti-pattern: vague "proposed file changes" section

**Symptom.** Plan lists "modify CLAUDE.md" without showing the exact diff.

**Fix.** Every file modification in the plan includes the exact diff or the
exact content block that will be added. The operator reads what will happen
before it happens.

### Anti-pattern: deferring too much

**Symptom.** Agent classifies most skills as deferred, leaving the
adopted set so thin that the kit provides no value.

**Fix.** Apply the mapping rubric honestly. A project with active development,
prose outputs, and iterative work should adopt at least 5-7 skills in Phase 1.
If the project genuinely has no use for most skills, that is correct -- but
document the reasoning per skill, not a blanket defer.
