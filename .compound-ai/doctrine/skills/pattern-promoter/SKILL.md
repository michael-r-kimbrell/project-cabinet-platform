# pattern-promoter (retired)
# Folded into the unified `memory` skill in the skill-merge. No independent session-routable skill.

Promoting a reusable pattern is now part of the `preserve` mode of `memory`,
including the ACE-scored promotion format (helpful=N / harmful=N).
Procedure: `doctrine/skills/memory/reference/preserve.md` (Promotion path section)

Triggers ("promote this pattern", "save this lesson", "make this reusable",
"this should be a standard", "ACE this pattern") now route to `memory`.
