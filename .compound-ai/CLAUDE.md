# Claude pointer

This project uses `AGENT.md` as the canonical operating contract. Every directory follows the same pattern: read `AGENT.md` in this folder, then walk down into the relevant tier.

Start with the root `AGENT.md`, then `_tiers.md` for the inheritance model.
